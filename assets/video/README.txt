Put the looping hero clip here:

  hero.mp4   (required — H.264, 1920x1080, no audio, under ~6 MB)
  hero.webm  (optional — smaller, tried first)

Until then the hero falls back to an animated CSS desert background.
See ../../README.md for encoding commands.
